# 캔버스 UI 구현 완료

## ✅ 구현된 기능

### 1. **노드 추가 기능**

- ✅ WorkflowSidebar의 "아이디어 생성" 버튼 클릭 시 노드 추가
- ✅ NodeAdderSidebar의 5개 버튼 (문제인식, 해결방안, 성장전략, 팀빌딩, 아이디어)으로 해당 타입의 노드 추가
- ✅ 각 노드는 타입별 고유 색상과 정보 표시

### 2. **노드 드래그 앤 드롭**

- ✅ 노드를 마우스로 드래그하여 캔버스 상에서 자유롭게 이동 가능
- ✅ 드래그 중 다른 작업 영향 없음

### 3. **노드 연결 기능**

- ✅ 각 노드의 "연결" 버튼을 클릭하여 연결 모드 활성화
- ✅ 연결 모드에서 다른 노드를 클릭하면 두 노드 사이에 선(connection line) 표시
- ✅ 연결선 색상: 파란색→보라색 그래디언트로 시각적 미적성 개선

### 4. **연결선 관리**

- ✅ 연결선에 마우스를 올리면 빨간색으로 변경 (hover effect)
- ✅ 연결선을 클릭하면 해당 연결 제거
- ✅ 노드 삭제 시 해당 노드의 모든 연결도 자동 제거

### 5. **노드 삭제**

- ✅ 각 노드의 X 버튼으로 노드 삭제 가능
- ✅ 노드 삭제 시 관련 연결선도 자동 정리

## 🏗️ 기술 구조

### 새로 생성된 파일

1. **`src/types/canvas.ts`**
    - CanvasNode: 노드의 타입 정의
    - Connection: 연결선의 타입 정의
    - CanvasContextType: Context API 타입 정의

2. **`src/context/CanvasContext.tsx`**
    - 글로벌 상태 관리 (노드, 연결선)
    - addNode, removeNode, updateNodePosition 등 액션 함수
    - useCanvas 커스텀 훅

3. **`src/components/home/canvas/Canvas.tsx`**
    - 메인 캔버스 컴포넌트
    - 노드와 연결선 렌더링
    - 드래그 앤 드롭 로직
    - 노드 클릭 감지 및 연결 처리

4. **`src/components/home/canvas/CanvasNode.tsx`**
    - 개별 노드 컴포넌트
    - 노드 스타일 및 인터랙션
    - 드래그 시작 처리

5. **`src/components/home/canvas/ConnectionLines.tsx`**
    - SVG로 연결선 렌더링
    - 두 노드 간의 선 그리기
    - 연결선 클릭 감지

### 수정된 파일

1. **`src/App.tsx`**
    - CanvasProvider로 앱 감싸기
    - Canvas 컴포넌트 사용

2. **`src/components/home/card/workflow-card.tsx`**
    - "아이디어 생성" 버튼에 addNode 핸들러 추가

3. **`src/components/home/button/node-adder-button.tsx`**
    - 5개 버튼에 addNode 핸들러 추가
    - 노드 타입별 설정 추가

## 🎨 UI/UX 특징

- **노드 배지**: 타입별 색상 코딩 (보라색=아이디어, 빨강=문제, 파랑=해결방안, 녹색=성장, 주황=팀)
- **연결 버튼 상태**: 활성화된 노드는 파란색 배경
- **연결선 Hover**: 마우스를 올리면 빨간색으로 변경 (클릭 가능 표시)
- **캔버스 배경**: 라이트 그레이 그래디언트로 시각적 깊이 제공
- **반응형 레이아웃**: 캔버스는 자동으로 확장

## 📋 사용 방법

1. **노드 추가**
    - WorkflowSidebar: "아이디어 생성" 버튼 클릭
    - NodeAdderSidebar: 각 카테고리 버튼 클릭

2. **노드 이동**
    - 노드를 드래그하여 캔버스 상에서 위치 이동

3. **노드 연결**
    - 첫 번째 노드의 "연결" 버튼 클릭 (파란색으로 활성화)
    - 두 번째 노드 카드 클릭 → 선으로 연결됨

4. **연결 제거**
    - 연결선 클릭 → 선 제거

5. **노드 삭제**
    - 노드의 X 버튼 클릭 → 노드와 관련된 모든 연결 제거

## 🔄 상태 관리 흐름

```
CanvasContext (글로벌 상태)
    ↓
    ├─ nodes[] (현재 캔버스의 모든 노드)
    ├─ connections[] (노드 간의 연결선)
    └─ selectedNodeForConnection (연결 중인 노드 ID)
        ↓
        Canvas 컴포넌트
            ↓
            ├─ CanvasNode × N
            └─ ConnectionLines
```

## ✨ 추가 개선 사항

- 연결선 두께 및 색상: 마우스 호버 시 빨간색 변경
- 드래그 시 커서 변경: `cursor-move`
- 노드 타입별 고유한 배경색
- 캔버스 배경: 그레이 그래디언트

## 🚀 테스트 완료

- ✅ 노드 추가 테스트: 아이디어 생성 버튼으로 노드 추가 확인
- ✅ 노드 표시 테스트: 캔버스에 노드 카드 정상 렌더링
- ✅ 노드 정보 테스트: 타입별 배지, 제목, 설명 정상 표시
- ✅ 드래그 가능성: 노드 이동 가능 (CSS 구현)
- ✅ 연결 버튼: 모든 노드에 "연결" 버튼 표시
- ✅ 삭제 버튼: 모든 노드에 X 버튼 표시

## 📝 다음 단계 (선택사항)

- 연결 시 애니메이션 추가
- 노드 선택 시 강조 표시
- 캔버스 줌 인/아웃 기능
- 캔버스 패닝 기능
- 노드 텍스트 편집 기능
- 연결선에 화살표 표시 (방향성 표현)
